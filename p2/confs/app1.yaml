apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1-deployment  # deployment name 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app1 # deployment label
  template:
    metadata:
      labels:
        app: app1 # pod label
    spec:
      containers:
        - name: app1
          image: paulbouwer/hello-kubernetes:1.10.1
          env:
            - name: MESSAGE
              value: "Hello from App 1"
          ports:
            - containerPort: 8080

---

apiVersion: v1
kind: Service
metadata:
  name: app1-service  # service name
spec:
  type: ClusterIP #default
  selector:
    app: app1 # Service label
  ports:
    - port: 80
      targetPort: 8080